import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  particlesWrapper: {
    position: 'absolute',
    bottom: 0,
    top: 0,
    width: '100%',
    zIndex: 1,
  },
});

export default styles;
